# - name: "Create Install the Github-actions"
#   hosts: webservers
#   tasks:
#     - add-action

- name: Run only add-action role
  hosts: webservers
  become: yes
  vars_files:
    - ../vars/github-actions.yaml
  roles:
    - add-action